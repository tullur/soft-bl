<%# remote: true %>
<%= form_for [@article, Comment.new], remote: true, class: "form-group" do |f| %>
  <%= f.text_area :body, placeholder: "Add a comment", class: "form-control" %> <br>
  <%= f.submit "Add Comment", class: "btn btn-primary", id: "comment" %>
<% end %>
